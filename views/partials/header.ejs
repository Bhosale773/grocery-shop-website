<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link id="icon" rel="icon" href="../images/icon.ico">
    <title>Grocery Shop</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.9.0/css/all.min.css" rel="stylesheet"  type='text/css'>
    <link rel="stylesheet" href="../stylesheets/app.css" type="text/css">
    <link rel="stylesheet" href="../stylesheets/lib/bootstrap.min.css" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Roboto,Merriweather&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg fixed-top">
        <a class="navbar-brand ml-5 " href="/"><img id="logo" src="../images/logo.png" alt="LOGO"> </a>
        <button class="btn navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span><i class="fas fa-bars"></i></span>
        </button>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="n-list navbar-nav ml-auto">
            <li class="home n-item">
              <a class="n-link" href="/"><i class="fas fa-home"></i> Home</a>
            </li>
            <li class="aboutus n-item">
                <a class="n-link" href="#"><i class="fas fa-address-card"></i> About Us</a>
            </li>
            <li class="contactus n-item">
              <a class="n-link" href="#"><i class="fas fa-phone-alt"></i> Contact Us</a>
            </li>
            <% if(!currentUser){ %>
              <li class="login n-item">
                <a class="n-link" href="/login"><i class="fas fa-sign-in-alt"></i> Sign In</a>
              </li>
              <li class="signup n-item">
                <a class="n-link" href="/signup"><i class="fas fa-user-alt"></i> Sign Up</a>
              </li> 
            <% }else{ %>
              <li class="n-item">
                <div class="avatar">
                    <img src="../images/Avatar/male_avatar.png" alt="Icon"> 
                    <%=currentUser.fname%>
                </div>
              </li> 
            <% } %>         
          </ul>
        </div>
    </nav>
    <% if(currentUser){ %>
      <a style="display: none" id="logoutbtn" href="/logout">Log Out</a>
    <% } %>

    <div class="container">
      <% if(error && error.length>0){ %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= error %>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
        </div>
      <% }; %>
      <% if(success && success.length>0){ %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= success %>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
        </div>
      <% }; %>    
    </div>
    